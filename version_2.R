
#Código generado en RStudio
version

system("git config --global user.name 'carmenaguir'")
system("git config --global user.email 'carmenaguir@gmail.com'")

system("git config --global --list")
system("notepad ~/.gitconfig")

system("git config --global --list")

# Para eliminar 
system("git config --global --unset user.name")

system("git config --global --list")
system("git config --global --unset-all user.name")

system("git config --list")
